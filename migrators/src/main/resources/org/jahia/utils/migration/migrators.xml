<migrators>
    <migrate from="6.6" to="7.0">
        <resources>
            <resource pattern="**/*.cnd">
                <replace pattern="richtext[ckeditor\.customConfig='.*']" with="richtext" messageKey="migration.removeCustomCKEditorConfig"/>
            </resource>
            <resource pattern="**/*.groovy">
                <replace pattern="currentNode\.resolveSite\.home" with="renderContext.site.home" messageKey="migration.changeSiteHomePageResolution"/>
            </resource>
        </resources>
    </migrate>
</migrators>